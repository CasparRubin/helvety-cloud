# P5 — E2EE proof

```text
@docs/architecture/ROADMAP.md — Implement P5 E2EE proof only. First: P4 fix-before-P5 (commit if needed; revoke EXECUTE on rls_auto_enable + set_updated_at; tighten grants). Then wire P2–P4 for one encrypted issue via /api/v1. Recovery export = one-shot offline — never log/POST.
```

**Before E2EE (if not already done):**

- Commit P4 artifacts if still untracked.  
- Revoke `EXECUTE` on `rls_auto_enable` + `set_updated_at` from PUBLIC/anon/authenticated.  
- Tighten vault-table grants to match `11_grants.sql` (no TRUNCATE/REFERENCES/TRIGGER for authenticated).  
- Apply to `qnoeiurmyyyuawkcifmw` only; re-check advisors.

**Done when:** Grant/RPC fixes on remote; encrypt → API → decrypt round-trip works; service role still only sees ciphertext; recovery warning shown; recovery key never leaves the device via our APIs.

**Don’t:** Sharing, billing, notes/contacts, full sync protocol; log/POST `exportRecoveryKey` output; redesign P4 schema beyond grant/RPC fixes.
