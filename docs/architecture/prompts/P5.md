# P5 — E2EE proof

```text
@docs/architecture/ROADMAP.md — Implement P5 E2EE proof only. Wire P2–P4 for one encrypted issue via /api/v1. Seal/wrap AAD bound; recovery export = key + wrap offline once — never log/POST.
```

**Done when (landed):** Encrypt → API → decrypt round-trip; seal + wrap AAD; recovery key + wrap shown once offline; grant/RPC harden + inline membership on remote; service role still only sees ciphertext.

**Don’t:** Sharing, billing, notes/contacts, full sync protocol; log/POST recovery material.
