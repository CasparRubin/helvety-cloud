# P1 — Scaffold

```text
@docs/architecture/ROADMAP.md — Implement P1 Scaffold only. Bun + Next.js + package stubs + supabase folder. No auth/crypto/migrations.
```

**Done when:** `bun install` and web app start locally; README points to ROADMAP.

**Don’t:** Auth UI, real crypto, real SQL schema, Stripe, Redis, Sentry, copy helvety.com UI, shadcn (P2).
