# P9 — Task stage board

## Build

```text
@AGENTS.md @docs/architecture/ROADMAP.md @docs/architecture/prompts/P9.md

Implement P9 only: horizontal stage columns (always shown); drag tasks between stages; order cards by priority (highest first); thin EntityListShell for notes/contacts/projects + column rows; paginate all tasks; no schema migration.
```

**Done when:** Empty stages visible; DnD persists `stage_id`; priority orders within columns; flat lists use the shell; icons/colors from existing ciphertext defs.

**Don’t:** Manual within-stage reorder; label/priority colors; schema changes; domain mega-list; AI.

## Review

```text
@AGENTS.md @docs/architecture/ROADMAP.md @docs/architecture/prompts/P9.md

P9 review only.

Check: all stages rendered; DnD → stage_id only; priority desc sort; pagination; thin shell only; no migration. PASS/FAIL.
```
