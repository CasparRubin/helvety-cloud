# P2 — Auth

```text
@docs/architecture/ROADMAP.md — Implement P2 Auth only. Init shadcn Base UI (not Radix). Supabase email OTP + passkeys; passwords off. No vault crypto.
```

**Done when:** User can create a session via OTP (+ passkey sign-in); shadcn is Base UI; no content decryption.

**Don’t:** PRF vault persistence, encrypting issues, PostgREST vault CRUD from client, Radix shadcn.
