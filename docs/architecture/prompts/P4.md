# P4 — Schema + API

```text
@docs/architecture/ROADMAP.md — Implement P4 Schema+API only. Before persisting wraps: AAD on wrapKey/unwrapKey + real userId for key_check. Declarative supabase schemas, migrations, types, /api/v1 stubs. Use Supabase MCP. Project qnoeiurmyyyuawkcifmw only.
```

**Crypto (before first wrap write):**

- Bind AAD on wrapped-key blobs (`wrapKey`/`unwrapKey` or via `encrypt`) — content already binds `table:recordId:field`; wraps currently omit AAD.  
- `key_check` AAD: replace `recordId: "self"` with real user id when wiring `PUT /api/v1/me/crypto`.

**Done when:** Migrations on remote; types committed; wrap/key_check AAD bound; health + crypto/workspace stubs work with auth.

**Don’t:** Full Linear UI, Stripe, browser `from('tasks')`, sharing UI. Never touch old project `bkdzeihxzvrkndjvyzye`.
